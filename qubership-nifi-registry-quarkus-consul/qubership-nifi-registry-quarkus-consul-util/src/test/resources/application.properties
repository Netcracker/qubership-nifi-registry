# Test Configuration
cloud.microservice.namespace=local
cloud.microservice.name=qubership-nifi-registry
quarkus.application.name=qubership-nifi-registry
quarkus.consul-source-config.m2m.enabled=false

# Consul Configuration
quarkus.consul-source-config.enabled=true
quarkus.consul-source-config.agent.url=${CONSUL_URL:http://localhost:18500}
quarkus.consul-source-config.properties-root=config/${cloud.microservice.namespace}/application,config/${cloud.microservice.namespace}/${cloud.microservice.name}

# Application Settings
quarkus.banner.enabled=false

# Logging Configuration
quarkus.log.level=${ROOT_LOG_LEVEL:INFO}
quarkus.log.console.enable=true
quarkus.log.console.format=%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{3.}] (%t) %s%e%n

# Disable live reload and dev mode features for production
quarkus.live-reload.instrumentation=false

# NiFi Registry Configuration Paths for Testing
nifi.registry.config.path=./conf/
nifi.registry.notification.path=./tmp/
